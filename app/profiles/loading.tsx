import { LoadingView } from '@/app/components/loading-view';

/** Profiles renders a two-column grid of profile cards. */
export default function Loading() {
  return <LoadingView variant="grid" rows={4} />;
}
